APPSMITH_BASE_URL=http://localhost:8080
APPSMITH_ADMIN_EMAIL=admin@example.com
APPSMITH_ADMIN_PASSWORD=replace_me

HEADLESS=true
STORAGE_STATE_PATH=playwright/.auth/appsmith-admin.json
EVIDENCE_DIR=evidence/runs

# OpenAI-only selector repair. The fixture fallback is used only when no key is present.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
